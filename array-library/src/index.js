/*eslint keyword-spacing: ["error", { "before": true }]*/
/*eslint-env es6*/
import { reduce } from "./modules/reduce/reduce";

console.log( reduce( [ 1, 2, 3 ], ( a, b ) => a + b ) );
console.log( 112 );